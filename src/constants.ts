export const THREAD_MESSAGE="message";
export const PENDING = "PENDING";
export const DONE = "DONE";
export enum status {
    PENDING = 0,
    DONE = 1
}

export enum codeStatus{
    error=500,
    success=200
}